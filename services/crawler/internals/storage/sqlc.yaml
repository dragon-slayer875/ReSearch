version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    # INFO: This path is relative to the mounted docker volumes. Check the top level Makefile for more info
    schema: "../migrations"
    gen:
      go:
        package: "database"
        out: "database"
        sql_package: "pgx/v5"
